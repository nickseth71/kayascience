

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}





<div class="main-blog-nav page-width section-{{ section.id }}-padding">
  <div class="blog-tabs">
    <ul>
      {%- for link in section.settings.blog_link.links -%}
      <li class="blog-filter">
        <a href="javascript:void(0)" class="blog_filter-link {% if forloop.first %} active{% endif %}" data-handle="{{ link.title | handleize }}">
          {{ link.title | capitalize | replace: '-', ' ' }}
        </a>
      </li>
      {%- endfor -%}
    </ul>
  </div>
</div>

  {% schema %}
{
  "name": "Blog Nav",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "link_list",
      "id": "blog_link",
      "label": "Blog Nav"
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 36
    }
  ],
  "presets": [
    {
        "name": "Blog Nav"
    }
  ]

}
{% endschema %}