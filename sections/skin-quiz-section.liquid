
<div class="skin-quiz__section-wrapper">
<div class="page-width">
  <div class="skin-quiz__content-wrapper page-width">
    <span>For</span>
    <div class="skin-type__content-wrapp">
      <select class="js_col_label skin-type-data">
        {% for i in (1..5) %}
          {% capture col_label  %}col_label{{i}}{% endcapture %}
           {% capture label_key %}col_trigger{{ i }}{% endcapture %}
          {% assign group_label = section.settings[label_key] | handle %}
          {% if section.settings[col_label] != blank %}
            <option value="{{ group_label }}">
              {{ section.settings[col_label] }}
            </option>
          {% endif %}
        {% endfor %}
      </select>
    </div> 

  <span>  I'm looking for</span>
    <div class="product-type__content-wrapp">
      <select id="product-type-select" class="js_col_list product-type-data">
        <option disabled selected>Any Product</option>
        {% for i in (1..5) %}
          {% capture col_listing %}col_list{{ i }}{% endcapture %}
          {% capture label_key %}col_trigger{{ i }}{% endcapture %}
          {% assign group_label = section.settings[label_key] | handle %}
          
          {% for collection in section.settings[col_listing] %}
            {% assign coll_obj = collections[collection.handle] %}
            {% if coll_obj %}
              <option value="{{ coll_obj.handle }}" data-group="{{ group_label }}">
                {{ coll_obj.title }}
              </option>
            {% endif %}
          {% endfor %}
        {% endfor %}
      </select>
    </div>
   <span> that targets 
any concern</span>


<div class="concern-type__content-wrapp">
<select id="product-concern-select" class="js_concern_list concern-type-data">
<option disabled selected>Any Concern</option>

</select>
</div>
{% if section.settings.search_btn_label != blank %}
<div class="search_btn_label_wrapp">
<div class="button js-search-products" data-product-type="" data-concern-type="">
    {{ section.settings.search_btn_label }}
</div>
</div>
{% endif %}
  </div>
</div> 
</div>
<script>
window.collectionsProductsMetafields = {
  {% for collection in collections %}
    "{{ collection.handle }}": [
      {% for product in collection.products %}
        {
          id: "{{ product.id }}",
          title: "{{ product.title | escape }}",
          metafield:  {{ product.metafields.custom.concern_filter.value | json }}
        }{% unless forloop.last %},{% endunless %}
      {% endfor %}
    ]{% unless forloop.last %},{% endunless %}
  {% endfor %}
};

document.addEventListener("DOMContentLoaded", () => {
  const skinTypeSelect = document.querySelector(".js_col_label");
  const productTypeSelect = document.querySelector(".js_col_list");
  const allOptions = Array.from(productTypeSelect.querySelectorAll("option[data-group]"));
  const concernSelect = document.getElementById("product-concern-select");

  // Filter product types based on selected skin type
  function filterProductTypes(selectedGroup) {
    productTypeSelect.innerHTML = '<option disabled selected>Any Product</option>';
    allOptions.forEach(option => {
      if (option.dataset.group === selectedGroup) {
        productTypeSelect.appendChild(option.cloneNode(true));
      }
    });
    concernSelect.innerHTML = '<option disabled selected>Any Concern</option>';
  }

  skinTypeSelect.addEventListener("change", function () {
    filterProductTypes(this.value);
  });

  if (skinTypeSelect.value) {
    filterProductTypes(skinTypeSelect.value);
  }

  // Only ONE event listener for concern dropdown logic
  productTypeSelect.addEventListener("change", function () {
    const selectedCollection = this.value;
    const products = window.collectionsProductsMetafields[selectedCollection] || [];

    const concernsSet = new Set();
    const searchProductButton = document.querySelector('.js-search-products');
    searchProductButton.setAttribute("data-product-type",selectedCollection);
   products.forEach(prod => {
    console.log('Metafield Raw:', prod.metafield); // debug

        if (Array.isArray(prod.metafield)) {
          prod.metafield.forEach(item => concernsSet.add(item.trim()));
        } else if (typeof prod.metafield === "string") {
          concernsSet.add(prod.metafield.trim());
        } else if (prod.metafield && typeof prod.metafield === "object") {
          Object.values(prod.metafield).forEach(val => concernsSet.add(val.trim()));
        }
      });
 
    concernSelect.innerHTML = '<option disabled selected>Any Concern</option>';

    if (concernsSet.size === 0) {
      concernSelect.innerHTML = '<option disabled selected>No concerns available</option>';
    } else {
      concernsSet.forEach(concern => {
        const option = document.createElement("option");
        option.value = concern;
        option.textContent = concern;
        concernSelect.appendChild(option);
      });
    }
  });

  //concern type selection start
  concernSelect.addEventListener("change", function(){
    const selectedConcern = this.value;
    const searchProductButton = document.querySelector('.js-search-products');
    searchProductButton.setAttribute("data-concern-type",selectedConcern);
  });
  //consern type selection end



  document.querySelectorAll('.js-search-products').forEach(button => {
  button.addEventListener("click", function() {
    const concern = document.getElementById("product-concern-select").value;
    const productType = document.querySelector(".js_col_list").value;

    // Construct search query string
    const searchQuery = `${productType}`;

    // Build the full Shopify search URL
    const url = `https://kayascience.com/search?sort_by=relevance&q=${encodeURIComponent(searchQuery)}&options[prefix]=last&filter.p.m.custom.concern_filter=${encodeURIComponent(concern)}`;

    // Redirect to that URL
    window.location.href = url;
  });
});

});
</script>



{% schema %}
{
  "name": "Skin Quiz section",
  "settings": [
    {
        "type": "header",
        "content": "Content [1]"
    },
    {
      "type": "text",
      "id": "col_trigger1",
      "label": "Trigger Text"
    },
    {
      "type": "text",
      "id": "col_label1",
      "label": "Label to Display"
    },
    {
      "type": "collection_list",
      "id": "col_list1",
      "label": "Collection List [For Product Type]"
    },
    {
        "type": "header",
        "content": "Content [2]"
    },
     {
      "type": "text",
      "id": "col_trigger2",
      "label": "Trigger Text"
    },
    {
      "type": "text",
      "id": "col_label2",
      "label": "Label to Display"
    },
    {
      "type": "collection_list",
      "id": "col_list2",
      "label": "Collection List [For Product Type]"
    },
     {
        "type": "header",
        "content": "Content [3]"
    },
     {
      "type": "text",
      "id": "col_trigger3",
      "label": "Trigger Text"
    },
    {
      "type": "text",
      "id": "col_label3",
      "label": "Label to Display"
    },
    {
      "type": "collection_list",
      "id": "col_list3",
      "label": "Collection List [For Product Type]"
    },
     {
        "type": "header",
        "content": "Content [4]"
    },
     {
      "type": "text",
      "id": "col_trigger4",
      "label": "Trigger Text"
    },
    {
      "type": "text",
      "id": "col_label4",
      "label": "Label to Display"
    },
    {
      "type": "collection_list",
      "id": "col_list4",
      "label": "Collection List [For Product Type]"
    },
    {
        "type": "header",
        "content": "Content [5]"
    },
      {
      "type": "text",
      "id": "col_trigger5",
      "label": "Trigger Text"
    },
    {
      "type": "text",
      "id": "col_label5",
      "label": "Label to Display"
    },
    {
      "type": "collection_list",
      "id": "col_list5",
      "label": "Collection List [For Product Type]"
    },
    {
      "type": "text",
      "id": "search_btn_label",
      "label": "Button Label",
      "default": "Discover your match"
    }
  ],
  "presets": [
    {
      "name": "Skin Quiz Section"
    }
  ]
}
{% endschema %}
